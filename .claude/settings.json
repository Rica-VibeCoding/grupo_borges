{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "t=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; [ -f \"$t/.claude/hooks/check-git-updates.sh\" ] || exit 0; exec bash \"$t/.claude/hooks/check-git-updates.sh\"",
            "timeout": 15
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "t=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; [ -f \"$t/.claude/hooks/psmux-canal-load-skill.sh\" ] || exit 0; exec bash \"$t/.claude/hooks/psmux-canal-load-skill.sh\"",
            "timeout": 5
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "t=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; [ -f \"$t/.claude/hooks/recado-entre-agentes.sh\" ] || exit 0; exec bash \"$t/.claude/hooks/recado-entre-agentes.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
